# fastapi-app

Detect owned demo for **Python / FastAPI**.

```markdown
[![Open in Detect](https://<DETECT_HOST>/badge.svg)](https://<DETECT_HOST>/open/github/detect-demo/fastapi-app)
```

Run locally: `uvicorn main:app --reload`

Stack signals: FastAPI import in `main.py`, `fastapi` in requirements/pyproject.
